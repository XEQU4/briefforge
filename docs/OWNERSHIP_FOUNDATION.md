# Ownership foundation

BriefForge records users, organizations, and organization/team memberships, with optional user and organization references on tasks and proposals. These fields establish storage relationships only: authentication, authorization, and ownership enforcement are not implemented. Existing records remain valid with nullable owner references. Authentication and access policy belong to a later phase.
